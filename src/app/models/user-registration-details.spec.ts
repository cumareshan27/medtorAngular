import { UserRegistrationDetails } from './user-registration-details';

describe('UserRegistrationDetails', () => {
  it('should create an instance', () => {
    expect(new UserRegistrationDetails()).toBeTruthy();
  });
});
